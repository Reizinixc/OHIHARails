<!DOCTYPE html>
<html>
<head>
  <title><%= title(@title) %></title>
  <%= stylesheet_link_tag "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<header id="header">
  <a href="/" id="logo">OHIHA | Online Handing in Homework Assistant</a>

  <div id="account_panel">
    <% if current_user %>
      <%= gravatar_for current_user, 30, 30 %>
      <%= link_to header_name, "/user/#{current_user.username}", :class => "username" %>
      <nav>
        <li><%= link_to t('settings'), "/settings" %></li>
        <li><%= link_to t('logout'), "/logout" %></li>
      </nav>
    <% else %>
      <nav>
        <li><%= link_to t('login'), "/login" %></li>
        <li><%= link_to t('signup'), new_user_path %></li>
      </nav>
    <% end %>
  </div>
</header>

<div id="wrapper">
  <% if flash.any? %>
    <div id="notice">
      <ul>
        <% flash.each do |key, value| %>
          <li class="<%= key %>"><%= value %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= yield %>
</div>

<footer id="footer">
  2012 OHIHA Project. Some rights reseved.<br/>
  The OHIHA Project is a part of Software Design Development &amp; System Analysis and Design.
</footer>

</body>
</html>
